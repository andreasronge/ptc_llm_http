# Development toolchain. Matches PtcRunner's current pair so an integration
# checkpoint compiles both projects under one runtime. CI additionally tests
# the declared minimum; see .github/workflows/ci.yml.
[tools]
erlang = "29.0.3"
elixir = "1.20.2-otp-29"
